"""Transcript source adapters behind TranscriptSourcePort (AD-23).

Two from day one: Graph as primary, and a manual watched folder so the meeting
pipeline stays developable and testable without tenant-admin consent — which is
outside this project's control.
"""
