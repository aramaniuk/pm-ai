---
scope: pm-ai — local-first AI PM assistant, full system (greenfield)
purpose: build-substrate + fuller solution design doc
altitude: initiative→features (whole-system spine from PRD v0.8.0 at docs/prd_pm_ai.md)
updated: 2026-08-20T16:40
---

- (constraint) [ADOPTED FR-36] LLM core has zero shell access; every external read/write routes through signed, statically-verified MCP skill modules. Prevents: units wiring direct API/shell calls around the firewall.
- (constraint) [ADOPTED NFR-14] Daemon binds strictly to 127.0.0.1; zero public listening ports. Telegram is the only off-host surface, via HTTPS webhook/polling with cryptographic user-ID pairing.
- (constraint) [ADOPTED NFR-11] Plain Markdown files are the source of truth; SQLite (event_telemetry.db) and vector_index/ are disposable derived indexes — full rebuild from Markdown must be possible with zero data loss.
- (constraint) [ADOPTED §2.1/FR-16] Dual-scope data ownership: ~/.manager-ai/ (sovereign personal, never committed) vs <repo>/.project-ai/ (committed). Private enclave ~/.manager-ai-private/ AES-256 encrypted, perms 600, gitignored.
- (constraint) [ADOPTED NFR-12/NFR-13] Local quantized 7B-13B via Ollama + Whisper small.en for parsing/transcription; frontier LLM API reserved for high-level synthesis; total spend <$20/month; 16GB Apple Silicon / 8GB VRAM baseline.
- (constraint) [ADOPTED FR-18/FR-19] Exactly two user surfaces: Telegram bridge (primary mobile) and terminal CLI REPL (loopback), full feature parity for text.
- (constraint) [ADOPTED FR-36.2] All inbound external telemetry passes a pre-parsing input-sanitization firewall before any LLM context ingestion.
- (event) Run started: coaching path; deliverables = spine + fuller solution design doc. Input: docs/prd_pm_ai.md v0.8.0 (greenfield).
- (decision) Runtime shape: ONE long-lived daemon owns all background life (telemetry radar, calendar triggers, Telegram bridge, job queue, pruning). CLI is a thin client talking to the daemon over loopback. Binds: all. Prevents: two processes independently owning schedules/SQLite writes; CLI and Telegram implementing divergent copies of the same features.
- (decision) IPC: CLI↔daemon over HTTP on 127.0.0.1 with a per-user token file (0600) for local auth; SSE for streaming responses. Binds: CLI client, daemon API surface. Prevents: each feature inventing its own IPC; unauthenticated local processes driving the daemon.
- (decision) Telegram transport: outbound LONG-POLLING only; webhooks explicitly out (would require a public HTTPS endpoint/tunnel, violating NFR-14 zero-public-ports). Binds: Telegram bridge. Prevents: a tunnel/cloud dependency creeping in as 'just for webhooks'.
- (decision) Language: Python for daemon, CLI, connectors, and skills (matches PRD's .py skill files; best ecosystem fit for Whisper/Ollama/MCP). Binds: all first-party code. Prevents: polyglot drift between skills and core.
- (question) Within Python: hand-rolled agent orchestration vs Claude Agent SDK (or similar) for MCP client plumbing + tool permissioning — open, awaiting user.
- (decision) Agent foundation: HYBRID. Deterministic/local majority (radar, sanitization, fuzzy anchor matching, Ollama parsing, ledger mechanics, scheduling) = plain Python, no framework. Frontier-synthesis flows ONLY (1:1 coaching, briefing synthesis, research tasks, draft generation) go through the Claude Agent SDK (Python), which also provides the MCP client for the signed-skill registry. Rule: only the synthesis layer may invoke the SDK; nothing else touches a frontier API. Binds: cost cap NFR-13, FR-36. Prevents: frontier calls leaking into cheap paths; two MCP client implementations.
- (decision) Design paradigm: HEXAGONAL (ports & adapters) around a plugin kernel. Core (extraction, ledger lifecycle, alignment, scheduling) is I/O-free; inbound adapters = hot-loadable connectors normalizing to Connector Schema with sanitization at the adapter boundary; outbound adapters = signed MCP skills (only egress) + storage layer. Ingestion runs as pipes-and-filters: harvest → sanitize → normalize → index → extract → stage/execute. Binds: all. Prevents: features importing connector/tool code directly; sanitization applied inconsistently per feature.
- (direction) Rationale: hybrid keeps the $20/mo cap enforceable at one architectural boundary; hexagonal was already implied by PRD §6 and makes 'new connector' and 'new MCP skill' the two standard extension points.
- (decision) Single-writer rule (approved): one storage service inside the daemon owns ALL writes (markdown, SQLite, vectors, encrypted blobs); no other component opens a file for writing. Markdown ledgers/logs are append-only (status changes = new entries keyed by id, never in-place edits). SQLite runs WAL with the storage service as sole writer. Binds: all. Prevents: two components racing on the same file/row; half-written ledger entries.
- (decision) Encryption scope (narrows NFR-08): only event_telemetry.db, chat_history/ (raw transcripts+audio), vector_index/, telegram_cache/, and config.json (API credentials) are ever encrypted. All .md files in BOTH scopes (~/.manager-ai/ incl. coaching_1on1_history.md, strategic_goals.md, event_log.md; <project>/.project-ai/ incl. commitments_log.md, engineering_specs.md, conventions.md) stay plaintext by design — transparency/sovereignty over the record is a product principle, not just a perf shortcut. Binds: storage service, NFR-08. Prevents: a future feature silently encrypting a .md file 'for consistency' and breaking git-diffability/manual editing.
- (decision) Encryption is toggle-able (config flag, default ON) for local debugging — when OFF, the storage service writes the same encryptable paths as plaintext instead of AES-256 blobs, same read/write API either way. Rule: the toggle is dev/debug-only, must warn loudly (CLI banner + event_log entry) when OFF, and is never the default in a fresh install. Binds: storage service. Prevents: a debug convenience silently becoming the shipped default.
- (decision) Master key: OS keychain (macOS Keychain / Secret Service via a cross-platform lib), enabling unattended daemon start (07:00 briefing) with no passphrase prompt. Export/import of the raw key is the documented portability escape hatch for machine migration. Binds: storage service key management.
- (question) Reconciliation flag for Finalize step 2: this narrows PRD NFR-08 ('all... Socratic coaching logs... must be encrypted') — coaching_1on1_history.md is now explicitly plaintext. Offer to update the PRD source (NFR-08 wording) so it doesn't silently diverge from the spine.
- (decision) Connector contract: every connector is a Python class implementing a single uniform pull method harvest(since: Cursor) -> list[NormalizedEvent]. Connectors own ONLY auth + fetch + map-to-Connector-Schema. They never run their own thread, timer, or polling loop; the daemon's scheduler invokes them on its cadence (4h default per FR-02) and owns cursors/backoff. Sanitization, dedup, indexing and storage happen outside the connector, uniformly. Binds: FR-02, FR-35, all connectors. Prevents: per-connector schedulers competing for rate limits; each connector inventing its own event shape or applying sanitization inconsistently.
- (decision) MCP skills are NOT cryptographically signed for now (diverges from PRD FR-36 wording 'signed, statically verified'). What REMAINS binding: the execution boundary itself — LLM core gets zero shell/raw terminal access, and 100% of external reads/writes route through the MCP skill layer, which stays the single egress point with every invocation logged to event_log.md. What is dropped: signature generation/verification at load time. Binds: FR-36, MCP layer. Prevents: teams reading 'unsigned' as 'the firewall is optional'.
- (question) DEFERRED with revisit condition: MCP skill signing/verification. Revisit when (a) any skill not authored by Andrei is installed, or (b) skills are shared/distributed to others. Until then the skill registry is a local allowlist of first-party modules. Keep the MCP layer's load path pluggable so a verification step can be inserted without restructuring.
- (question) Reconciliation flag #2 for Finalize step 2: PRD FR-36 + Non-Goals + Glossary repeatedly say 'signed, statically verified MCP tools'. Offer to update the PRD wording to match (execution boundary binding; signing deferred).
- (decision) Approval: ONE generic Proposal entity in core — id, type, summary, payload, target executor, expiry, status (staged → approved → executed | edited | rejected | expired). Persisted by the storage service; rendered by ONE card renderer with two backends (Telegram inline keyboard, CLI approval queue). Features never build approval flows; they REGISTER a proposal type (payload schema + executor callback). Binds: FR-06, FR-21, FR-31, FR-34, UJ-9. Prevents: five divergent approval mechanisms/card formats/expiry semantics.
- (decision) FR-34's commitment lifecycle (PENDING → FULFILLED/ALTERED/BROKEN) is a DOMAIN state machine, distinct from the Proposal lifecycle. A proposal approval merely feeds it (STAGED_APPROVAL is a proposal state; PENDING onward is domain state). Prevents: conflating approval status with real-world fulfillment status in one overloaded field.
- (decision) THREE-SCOPE model (supersedes PRD §2.1's two scopes). (1) ~/.pm-ai/ = APPLICATION/SYSTEM level: daemon settings, project registry, per-project connector configuration, global defaults. (2) ~/.manager-ai/ = SOVEREIGN PERSONAL: coaching, career, leadership principles, strategic goals, personal briefings — contains NO project-specific information or configuration whatsoever. (3) <repo>/.project-ai/ = committed per-project scope: rules, personas, conventions, dashboards, commitments ledger, project event log. Binds: all storage paths. Prevents: project config leaking into the personal sovereign scope and breaking its portability across role/company transitions.
- (decision) Project registration is EXPLICIT via 'pm-ai project add' (no filesystem scanning/auto-discovery). The registry lives in ~/.pm-ai/. CLI invoked inside a registered repo auto-binds to that project scope; Telegram (no cwd) requires explicit project selection or a configured default. Binds: FR-35, CLI, Telegram bridge.
- (decision) Connector instances are PER-PROJECT, not global. Each registered project gets its own independently-scheduled harvesting: git/GitLab (work items, wiki, MRs, CI/CD events) by default, plus optional additional sources (Teams, Outlook, Slack, Jira, Notion) enabled per project independently. A connector instance = (scope, connector_type, config, cursor) with its own cadence and cursor; the daemon scheduler owns all of them. Personal-scope connector instances (HR tools, article sources, personal calendar) exist separately under the personal scope. Binds: FR-02, FR-35. Prevents: a single global connector config assumed by one builder while another assumes per-project; shared cursors causing cross-project gaps/duplication.
- (decision) Configuration mode: connector add/enable/disable/test is reachable from BOTH CLI and Telegram (config mode), operating on the same registry in ~/.pm-ai/ through the daemon — no surface-specific config store. Binds: FR-35, UJ-10.
- (question) Reconciliation flag #3 for Finalize step 2: PRD §2.1, §6 topology, and many FR paths assume two scopes with config.json under ~/.manager-ai-private/. Spine now uses three scopes with app+project config under ~/.pm-ai/. Offer to update PRD §2.1/§6 accordingly.
- (question) OPEN: private-enclave placement under the three-scope model — does the encrypted enclave split (operational under ~/.pm-ai/private/ vs personal under ~/.manager-ai-private/) or consolidate? Awaiting user.
- (decision) Enclave SPLIT into two encrypted stores. (1) ~/.pm-ai/private/ = operational: connector credentials, event_telemetry.db (work/project telemetry + job queue + commitment index), vector_index/, chat_history/ (transcripts, audio), telegram_cache/. (2) ~/.manager-ai-private/ = personal-only derived telemetry: burnout metrics, workload/calendar-density dynamics, coaching analytics. Rule: project-scope rendering NEVER opens the personal store; the personal store is a separate SQLite DB so personal analytics cannot be joined into any project-scope output. Binds: FR-16 privacy charter, NFR-07, storage service. Prevents: FR-16 being enforced only by a remembered tag check — the wall is structural.
- (decision) Model routing: ONE ModelRouter port in core, two adapters (local: Ollama + Whisper; frontier: via Claude Agent SDK). No feature instantiates a model client directly. Routing is by DECLARED TASK CLASS at the call site: transcription | extraction | classification | embedding | fuzzy-match => LOCAL ONLY, always. coaching | briefing-synthesis | research | draft-generation => frontier-eligible. Binds: NFR-13, NFR-12, all model use. Prevents: a cheap path silently calling a frontier model; per-feature model clients bypassing accounting.
- (decision) Cost cap is MONITORING, not enforcement (diverges from NFR-13 'must remain capped strictly'). The router keeps a budget ledger — every frontier call logged with token counts + cost estimate to event_log.md, monthly running total surfaced in briefings/CLI. At threshold breach: WARN only. No degradation to local, no hard stop, no feature blocking. Rationale: at this stage the $20 figure is for understanding real efficiency and limits, not for restricting the system. Binds: ModelRouter. Prevents: a builder implementing silent quality degradation or feature cutoff that Andrei never asked for.
- (question) Reconciliation flag #4 for Finalize step 2: PRD NFR-13 + SM-5 state the $20/month figure as a hard cap ('must remain capped strictly'). Spine treats it as a monitored target with warn-only behavior. Offer to reword NFR-13/SM-5.
- (decision) Concurrency: single asyncio event loop owns ALL I/O (connector harvests, Telegram long-poll, loopback HTTP API, MCP calls). CPU/GPU-bound work (Whisper transcription, embedding generation, local Ollama inference) runs in a BOUNDED worker pool, never on the loop. Default: at most ONE heavy local-model job concurrently (directly addresses NFR-12 16GB baseline + PRD Open Question #1 on swap thrashing); the bound is configurable. Binds: all background work. Prevents: a 30s transcription making the Telegram bridge and CLI unresponsive; unbounded parallel model loads thrashing 16GB unified memory.
- (decision) Durable work rule: EVERY unit of deferred work is a persisted row in the SQLite job queue — nothing is scheduled in memory only. FR-04's offline buffer is not a separate mechanism, it is the same queue in a PENDING_RETRY state. Jobs are at-least-once with mandatory idempotency keys so replay after crash/reconnect cannot double-execute an external mutation (e.g. posting the same WI comment twice). Binds: FR-02, FR-04, FR-07, NFR-10. Prevents: in-memory timers losing work on restart; duplicate external writes on replay.
- (decision) Platform: macOS ONLY for v1 (Apple Silicon). Linux is a later target; keep OS-touching concerns behind ports (keychain access, process supervision, packaging paths) so Linux adapters slot in without restructuring. Binds: install/supervision/keychain. Prevents: macOS APIs called directly from core code, making the later Linux port a rewrite.
- (decision) Supervision & install: launchd USER agent on macOS (KeepAlive, start at login); uv tool install / pipx-style isolated install; 'pm-ai doctor' is the single health command (keychain access, Ollama reachability, per-connector probe status, index/disk sizes). Binds: operational envelope.
- (decision) Backup & recovery follows markdown-is-truth: back up the markdown scopes only (project scope rides in git; personal scope may be its own private git repo) plus an exported keychain key. Encrypted indexes are explicitly NOT a backup target; 'pm-ai reindex' rebuilds event_telemetry.db and vector_index/ from markdown. This operationalizes NFR-11. Binds: storage service, NFR-11.
- (decision) Log separation (hard rule): event_log.md is DOMAIN TRUTH — decisions, actions, telemetry events; append-only, human-readable, never rotated, never carries debug output. Diagnostic/debug logging goes to rotating structured JSON under ~/.pm-ai/logs/. Binds: all components. Prevents: debug noise polluting the audit trail and destroying event_log.md's value as a decision record.
- (decision) Split latency SLA (resolves PRD FR-37 vs NFR-04 contradiction). RETRIEVAL (SQLite + vector lookup, no LLM in path): 50-150ms — this is the real FR-37 budget. SYNTHESIS (retrieval + LLM): <=60s, always asynchronous. Binds: FR-23, FR-25, FR-32, FR-33, FR-37, NFR-04. Prevents: a builder targeting 150ms for an LLM-synthesized answer, which is unachievable, or blocking a surface while waiting.
- (decision) Async response invariant: ANY request whose expected duration exceeds 5 SECONDS returns an acknowledgement token + job id immediately and delivers its result later over the same async channel (Telegram message or CLI notification). Requests under 5s may respond inline. One pattern across both surfaces. Binds: Telegram bridge, CLI, job queue, UJ-5. Prevents: some flows blocking and others acking, so users/builders can't predict behavior; long requests holding an HTTP/poll connection open.
- (decision) Transcript acquisition behind a TRANSCRIPT-SOURCE PORT with multiple adapters. Primary: Microsoft Graph transcript connector (verified 2026-08: personal MS accounts unsupported; application permissions require a tenant admin to create an application access policy granting the app rights over a user's meeting artifacts; delegated needs OnlineMeetingTranscript.Read.All; transcripts exist only when recording/transcription was enabled). Fallback (built from day one): manual drop of a .vtt/.docx/.txt transcript into a watched folder, plus a local-recording + Whisper path. Binds: FR-03, FR-06, FR-07, FR-08, UJ-3/6/7/8. Prevents: the entire meeting pipeline being blocked on tenant admin consent, and makes the pipeline testable with no live tenant.
- (constraint) RISK (external, unresolved): Graph transcript access depends on tenant admin cooperation (application access policy / admin consent). User expects it to be obtainable but it is outside project control. Mitigated by the fallback adapter above.
- (version) Verified 2026-08-18 via web: sqlite-vec = embedded, server-less, SQL-native, EXACT brute-force search (cost linear in row count) — fits single-file storage + single-writer + rebuildable-index invariants better than Chroma (which is Python-first with approximate indexing and a separate process). Linear cost is acceptable because FR-37 pruning caps index size.
- (decision) CORRECTION to earlier hybrid decision: use the Anthropic Python SDK's TOOL RUNNER (client.beta.messages.tool_runner + @beta_tool), NOT the Claude Agent SDK. Rationale: the Claude Agent SDK is Claude Code as a library and ships built-in Bash/Read/Write/Edit/Glob/Grep tools — structurally incompatible with FR-36's execution firewall. The Tool Runner loops only over tools we define: no built-in tools, no filesystem access, no sandbox. MCP skills convert to tool-runner tools via anthropic.lib.tools.mcp (mcp_tool / async_mcp_tool; requires anthropic[mcp], Python 3.10+). Binds: FR-36, ModelRouter frontier adapter. Prevents: a library's default toolset silently reintroducing shell access the firewall exists to forbid.
- (version) Verified 2026-08-18 via claude-api skill: current models are Claude Opus 5 (claude-opus-5, $5/$25 per Mtok, 1M ctx), Claude Sonnet 5 (claude-sonnet-5, $3/$15; intro $2/$10 through 2026-08-31), Claude Haiku 4.5 (claude-haiku-4-5, $1/$5, 200K ctx). PRD §6 names 'Claude 3.5 Sonnet' — retired Oct 2025. Reconciliation flag #5.
- (version) Verified 2026-08-18: Opus 5 API specifics that bind the frontier adapter — thinking is ON by default (omitting the param runs adaptive); max_tokens caps thinking+text together; temperature/top_p/top_k are REJECTED (400); assistant prefill rejected (400); use output_config.format for structured output; effort via output_config.effort (low..max). Prompt-cache minimum is 512 tokens on Opus 5 (1024 on most others) — relevant to the cost target since persona/rules prefixes repeat across briefings.
- (version) Verified 2026-08-18 via web: python-telegram-bot 22.8 (released 2026-06-12), pure-Python asyncio, supports Python 3.10+, Bot API 10.0.
- (question) Model-tier choice for the frontier synthesis layer is a user decision (cost vs depth) — surfacing to user rather than assuming.
- (decision) Frontier model tiering by task class: coaching (FR-12/14/15) + deep research (FR-07) => claude-opus-5; briefing synthesis (FR-09/32), dashboards, draft generation (FR-21), inquiry synthesis (FR-23) => claude-sonnet-5. Expressed as a config table in the ModelRouter, not new machinery. Binds: ModelRouter frontier adapter.
- (decision) Vector index is NOT encrypted (further narrows NFR-08). ~/.pm-ai/private/vector_index/ holds embeddings only, protected by 0600 perms + FileVault. Rationale: removes the unverified SQLCipher+sqlite-vec extension-loading integration risk entirely; embeddings are derived, rebuildable, and hold no raw text. SQLCipher still encrypts event_telemetry.db (needs no extensions). Binds: storage service, NFR-08. Prevents: a Phase 1 blocker on an undocumented integration.
- (version) Verified 2026-08-18 via web: whisper.cpp is the correct choice on Apple Silicon — Metal + Core ML backends, ~3x faster than faster-whisper on Mac (faster-whisper has no Metal backend, runs CPU-only there); sub-300ms cold start vs ~1.8s. faster-whisper would only win on NVIDIA, which is out of scope for macOS-only v1.
- (version) Verified 2026-08-18 via web: Python 3.14 released 2025-10-07, 3.14.6 on 2026-06-10; common guidance is 6-12 months before production adoption (that window has passed). Pinning 3.13 for v1 as the conservative choice given sqlcipher3 / sqlite-vec native-wheel maturity; 3.14 noted as the upgrade path.
- (version) Verified 2026-08-18 via web: sqlite-vec = server-less, SQL-native, exact brute-force KNN. RISK FOUND: SQLCipher + sqlite-vec extension loading is undocumented, and Python's stdlib sqlite3 frequently lacks enable_load_extension on macOS system Python (Homebrew Python is the usual workaround). Resolved by NOT encrypting the vector index (see decision above).
- (question) Reconciliation flag #5: PRD §6 topology names 'Claude 3.5 Sonnet' (retired 2025-10-28) and 'Ollama 7B-13B'. Offer to update to Opus 5 / Sonnet 5 tiering.
- (question) Reconciliation flag #6: NFR-08 now narrowed twice — .md files plaintext by design AND vector_index/ unencrypted. Offer to rewrite NFR-08 to the exact encrypted set: event_telemetry.db, chat_history/, telegram_cache/, config.json.
- (event) Reviewer gate run (lint + rubric + 2 configured lenses, inline per user instruction to avoid subagents). lint_spine.py: 0 findings. Adversarial lens found 5 real divergence holes, all closed: (1) open NormalizedEvent/event_log taxonomies => AD-27 closed enumerations; (2) commitments vs coaching commitments could share storage => AD-28; (3) sanitization could overwrite raw evidence and break source_refs => AD-29; (4) Cursor semantics undefined => AD-9 tightened to opaque-to-core; (5) idempotency key derivation unspecified, could be random => AD-20 tightened to deterministic sha256(job_type+target_ref+canonical_payload). Proposal expiry ownership + default TTL added to AD-13.
- (version) Verified 2026-08-18 via web: FastAPI 0.136.1; uvicorn 0.52.3 (released 2026-08-13, Python >=3.10); keyring 25.7.0 with macOS Keychain backend (macOS 11+, universal2 build required).
- (event) Spine finalized (status: final, 29 ADs, lint 0 findings). SOLUTION-DESIGN.md written as the second deliverable: rationale, 6 PRD divergences, 3 flow walkthroughs, cost model, risks/spikes, roadmap dependencies.
- (event) Enforcement layer built and verified: .importlinter (8 contracts, all KEPT; verified BROKEN on planted layering + core-IO violations), tests/architecture/ (34 checks - 11 active AST/layering, 23 behavioural stubs awaiting Phase 1 modules; verified catching planted violations with file:line). 8-package skeleton created per spine source tree so contracts run from day one. Spine gains an Enforcement section; Phase 1 exit criterion = zero skips.
- (decision) ADs that stay human judgement (documented in tests/architecture/README.md, not silently assumed covered): AD-4 scope ownership, AD-10 per-project cursor isolation, AD-12 sanitize-every-path, AD-18 allowlist contents, AD-19 pool bounds. Green CI does not mean fully compliant.
- (event) Independent reviewer gate complete: 4 fresh-context subagents (adversarial 22, divergence 17 pairs, implementer 62/14-blocking, tech-currency 20). Gate verdict: spine NOT build-ready. Reviews + consolidated TRIAGE.md in reviews/. ~121 findings collapse to 12 root causes; 3 require product decisions (frontier data boundary, spoken-command authorization, evidence retention vs citations).
- (assumption) CORRECTION: 'Llama 3.3 8B Q4_K_M' in the Stack table was invented - Llama 3.3 ships 70B only (verified against ollama.com/library/llama3.3/tags). Real 8B-class options: llama3.1:8b, qwen3:8b, gemma3:12b. Stack header claimed 'Verified current at 2026-08-18' which was not true of that row.
- (decision) CORRECTION: the SQLCipher + sqlite-vec integration risk that justified leaving vector_index/ unencrypted DOES NOT EXIST - tech reviewer verified the combination works by execution. The plaintext-index decision may stand on other merits (rebuildable, embeddings-not-text, perf) but its stated rationale in AD-6 and the PRD v0.9.0 decisions log is wrong and must be restated.
- (question) Real remaining risk found by tech review: sqlite-vec cannot load into stock macOS Python (enable_load_extension ABSENT, not disabled); uv-managed interpreters work. Requires --managed-python pin + a pm-ai doctor probe.
- (change) C11 stack corrections applied (self-verified against PyPI JSON and ollama.com, not taken on the reviewer's word): llama3.3 confirmed 70B-only across all 14 tags, smallest 26GB - fabricated pin replaced with a model CLASS plus verified candidates llama3.1:8b / qwen3:8b; FastAPI 0.136.1 -> 0.141.1 (confirmed); sqlite-vec pinned ==0.1.9 with pre-1.0 caveat (confirmed, summary is literally 'TODO readme'); sqlcipher3 annotated NOT sqlcipher3-binary (Linux-only); Core ML deferred, Metal only; Ollama client + embedding model/dimension added as Phase 1 pins.
- (change) New Stack subsections: Integration prerequisites (sqlite-vec needs uv-managed interpreter - enable_load_extension ABSENT on system macOS Python; Ollama server-side residency config; PTB manual lifecycle, run_polling prohibited; sqlite-vec minor bump = reindex event), Anthropic API notes (Opus 5 refusal handling + fallbacks; cache minimum differs 512 Opus 5 / 1024 Sonnet 5 and briefings run Sonnet 5; tool_runner beta acknowledged), Cost-model caveat ($20 target anchored to Sonnet 5 intro pricing expiring 2026-08-31).
- (change) AD-19 tightened: client-side pool bound is explicitly only half the guard; daemon must also constrain the Ollama server. No library may seize the event loop.
- (change) False-rationale correction propagated to all three documents (spine AD-6 context, SOLUTION-DESIGN §2 + divergence table, PRD v0.9.0 decisions log): the SQLCipher+sqlite-vec incompatibility cited as justification for leaving vector_index/ unencrypted was disproved by execution. Decision stands on other merits; stated reason was wrong.
- (event) Spine frontmatter status: final -> revision-in-progress. Open Risks gained 4 entries incl. an explicit 'not build-ready until triage clusters closed' statement. lint_spine 0 findings; 8 import contracts KEPT; 11 passed / 23 skipped.
- (decision) D1 RESOLVED (frontier data boundary): the FR-16 charter's adversary is EMPLOYER-CONTROLLED SYSTEMS, not model APIs. Personal-scope material MAY enter a frontier prompt. Two obligations follow: (a) FR-16 is reworded to name that threat model explicitly, so the charter says what it means; (b) every frontier call records the SCOPE PROVENANCE of its material to event_log.md, queryable from the CLI - 'what has left this machine' becomes auditable rather than asserted. New AD to be written in the revision pass; no silent per-feature decisions permitted.
- (decision) D2 RESOLVED (spoken-command authorization): auto-execute requires THREE conditions together - (1) transcript from a provider-authenticated source, (2) speaker identity resolves to the PM, (3) verb is reversible (comment, note, requirement edit, label). Any condition failing => stage as a Proposal. Irreversible verbs (outbound email/DM per FR-26, MR/PR creation per FR-28, closures) ALWAYS stage regardless of source. AD-23's manual watched folder is NEVER an auto-execute source - untrusted by construction; it remains a valid ingestion path for extraction and staging. Every auto-execution pushes a card with one-tap undo. Preserves UJ-7's zero-friction promise for the reversible common case; closes the unauthenticated-write hole.
- (decision) D3 RESOLVED (evidence retention) - reframed by the PM, and the reframe dissolves the finding rather than mitigating it. Transcripts are RAW MATERIAL: an input consumed by the extraction pipeline, spent once processed, retained 30 days only for exceptional tracing/debugging. NFR-09 stands unchanged; no evidence-span machinery; no retention extension. Commitment fulfilment checks commitments_log.md + Git/ticket telemetry (as FR-33/FR-34 already specify), never raw transcripts. Reviewer finding #17 was overstated by me: I repeated it without checking it against what the FRs actually say.
- (decision) D3 corollary - CITATION MODEL CORRECTED. A transcript is itself a derivative of the meeting, so facts must cite the MEETING, not the transcript. General rule: source_ref points at the most upstream DURABLE referent (the thing that happened), never at a derived capture of it. Commit SHAs, ticket anchors, message IDs already comply; the transcript was the sole violation. Consequences: (a) the dangling-pointer problem ceases to exist - meetings never expire; (b) my proposed durable/transient source_ref split is DROPPED as unnecessary; (c) AD-23's Graph and manual paths now yield identical provenance for the same statement, where transcript-citation would have encoded the ingestion path into the ref.
- (decision) Meeting becomes a first-class durable record (id, calendar event, title, datetime, duration, attendees, derived-transcript pointer) in markdown. Facts cite meeting:<id> + speaker + optional time offset. Tracing walks fact -> meeting -> transcript-if-present, preserving the debugging path without making it a dependency. Meeting also supplies Man-Hour Cost inputs for FR-03/FR-32/UJ-8, consolidating three ad-hoc lookups. NOTE: the ERD in the Structural Seed already modelled MEETING ||--o{ TRANSCRIPT; the citation convention incorrectly cited the middle node.
- (constraint) Manual transcript drop (AD-23 fallback) MUST bind to a meeting: to the calendar event where one exists, else the drop supplies title/date/attendees to mint the meeting record. Prevents an unattributed file minting attributed provenance - the same weakness D2 closed on the execution axis, now closed on the provenance axis.
- (event) D1-D3 all resolved. Revision pass unblocked: 8 of 12 triage clusters were waiting on these answers.
- (decision) BATCH 1 / C1 - AD-1 rewritten as five egress CLASSES with one legal home each: M mutation (skills/MCP only, authorized+idempotent+logged), H harvest (connectors, read-only by construction), F model (frontier adapter; can only cause an effect by emitting a tool call that re-enters M), S surface (Telegram outbound to paired user), L local subprocess (models.local only, whisper.cpp, allowlisted absolute path, argv list, shell=False, NO model output interpolated into argv). Invariant restated as: the model's ONLY route to an external effect is class M. The prior '100% of reads and writes route through MCP' was stricter-sounding and contradicted by the connector, frontier, and transcription paths - an unenforceable rule gets weakened to nothing on first contact.
- (decision) BATCH 1 / C5 - AD-3 rewritten as three storage tiers. T1 Truth (markdown, append-only, backup target, FR-37-compacted). T2 Operational (job queue + PENDING_RETRY, cursors, executed-idempotency-key ledger, staged proposals, keys) - durable and NOT derivable from T1; must be backed up; discarding it is a separate named CLI operation that states its consequences. T3 Derived (indexes, vector_index, caches) - disposable. 'pm-ai reindex' rebuilds T3 only; NFR-11's zero-loss guarantee scopes to T3. Resolves the contradiction where the documented recovery path would have silently dropped pending external writes.
- (decision) BATCH 1 / C6 - AD-30 NEW: pm_ai.app is the composition root and the only module permitted to import every layer. It constructs adapters, injects them through ports, routes credentials from encrypted storage to connectors, orchestrates the ingestion pipeline, owns the daemon lifecycle. Core services receive dependencies, never construct or locate them. Surfaces reach adapters only through core. Also adds pm_ai.domain (entities, enums, state machines, closed taxonomies; imports nothing from pm_ai) so ports can express protocols in domain types without a cycle - the previous layering had no legal home for the named pipeline at all.
- (change) Enforcement realigned with Batch 1: .importlinter gains app (top) + domain (bottom) layers and 3 contracts - surfaces-through-core (layering alone permitted surfaces->adapters, which was not the intent), domain-imports-nothing, subprocess-confined (class L carve-out). AST rule for AD-1 now permits subprocess in models.local ONLY, still rejecting shell=True and os.system/popen/eval/exec there. AD-5 write-scan widened to app+domain. 11 contracts KEPT, 11 passed / 23 skipped, spine lint 0.
- (decision) BATCH 2 / D1 - AD-31 NEW (model data boundary): FR-16's adversary is employer-controlled systems, not model APIs; personal scope MAY enter a frontier prompt. Three obligations: (1) FR-16 must be reworded to name that threat model - a charter meaning something narrower than its words invites false assurance; (2) every frontier call records scope provenance (scopes, task_class, model, token counts) to event_log.md, so the CLI can answer 'what has left this machine, and when' - converts the charter from assurance to audit; (3) the boundary is on DESTINATION as well as source - personal material must never enter a prompt whose output is bound for a project artifact or external system.
- (decision) BATCH 2 / D2 - AD-32 NEW (spoken-command authorization): auto-execute requires provider-authenticated source AND speaker resolves to PM AND verb in the reversible closed set. Any condition unmet => Proposal. Irreversible verbs (email/DM FR-26, MR creation FR-28, closures, deletions) ALWAYS stage regardless. Manual transcript adapter is never an auto-execute source. Every auto-execution emits an undo card + event_log entry.
- (decision) BATCH 2 / D3 - AD-33 NEW (citation model): source_ref points at the most upstream DURABLE referent, never a derived capture. Meeting-derived facts cite meeting:<id> + speaker (+optional offset for tracing). Meeting becomes a first-class Tier-1 record and the home of FR-03's Man-Hour Cost inputs, consolidating FR-03/FR-32/UJ-8 onto one entity. Derived records are self-contained, which is what makes NFR-09's purge purely operational. AD-23 amended: every ingested transcript binds to a Meeting or is rejected.
- (change) Batch 2 wiring: ERD gains MEETING||--o{EXTRACTION:cited_by; Citations convention rewritten to the durable-referent rule; capability map gains rows for in-meeting authorization and provenance; 8 new enforcement tests (AD-31 x2, AD-32 x2 incl. 5 parametrised cases, AD-33 x2, AD-23 binding); README coverage table updated. Spine lint 0; 11 contracts KEPT; 11 passed / 34 skipped.
- (decision) BATCH 3 / C2 - AD-34 NEW (event identity): fixes three elements in domain. (1) source_ref grammar <system>:<scope>:<kind>:<native_id>, free-form rejected at normalization. (2) actor_id resolved through an alias table; an unresolvable handle becomes the explicit UNRESOLVED actor, never a raw string used silently as identity - that is how one engineer splits into four in FR-30 metrics feeding a performance review. (3) dedup on the natural key (source_system, source_ref); the evt_ ULID is a surrogate minted by STORAGE at persist, connectors never mint ids, so re-harvest is idempotent.
- (decision) BATCH 3 / C3 - AD-35 NEW (time): occurred_at governs domain reasoning (due dates, causality), ingested_at governs operational reasoning (cursors, watermarks, sweeps); never substituted. Implausible/absent occurred_at is flagged, not backfilled. Ledger folds by (occurred_at, entry_id) - a total order stable across rebuilds, never file order, else reindex silently changes commitment states while AD-3's test passes. NEW: scheduler records harvest COVERAGE WINDOWS and the sweeper must not declare BROKEN across a gap - a sleeping laptop is missing data, not a broken promise, and FR-26 nudges are irreversible so this fails closed.
- (decision) BATCH 3 / C4 - AD-36 NEW (no self-verification): every event carries authored_by in {pm_ai, external}; commitment verification admits ONLY external evidence; FULFILLED requires evidence authored by someone other than pm-ai. Attribution established at BOTH ends - the skill layer records every class-M mutation (target ref + resulting external id) in the Tier-2 executed-key ledger, and actor resolution independently marks a distinct bot identity. Two mechanisms because one will have gaps. Closes the worst finding of the review: the executor's own comment being read back as fulfilment evidence.
- (decision) BATCH 3 / C7 - AD-37 NEW (CAS): Proposal and Commitment transitions are versioned compare-and-swap; conflict means reload-and-re-evaluate, never blind retry. Workers re-check state AT EXECUTION time, not only at enqueue. Expiry and execution are mutually exclusive by construction (sweeper CAS staged->expired, worker CAS approved->executing, loser observes and stops). Mutations on one external entity serialize by target_ref.
- (change) Batch 3 enforcement: 10 new tests (AD-34 x3, AD-35 x3, AD-36 x2, AD-37 x2). Reviewer-identified gaps in MY OWN test layer closed: AD-20 determinism now forks a subprocess (the single-process version passed a time.time() seed); AD-5 gains an append-only ledger check for the storage layer the write-scan exempts; test_ad8 switched from Flask test_client() to Starlette TestClient; .importlinter gains a ports forbidden-import contract. 12 contracts KEPT, 12 passed / 44 skipped, spine lint 0.
- (event) Run-2 divergence lens complete (2 reviewers, isolated workspace, run-1 prompt verbatim). R2-A: 19 pairs, 6 AD-vs-AD contradictions. R2-B: 21 pairs, 3 contradictions. Run 1 was 17. The revision CLOSED run-1's holes but opened new ones at the SEAMS BETWEEN the ADs added in batches 1-3. This is the third outcome we pre-agreed: semantics still weak, and more ADs is not the answer.
- (assumption) VERIFIED reviewer claims against my own artifacts: (1) TWO SourceRef types with incompatible grammars - domain.citations.SourceRef (batch 2, AD-33) vs domain.identity.SourceRef (batch 3, AD-34); (2) TWO incompatible auth.classify() signatures across adjacent tests, one can never pass; (3) NO pyproject.toml - both README-documented commands (uv run pytest / uv run lint-imports) FAIL; my green reports were true only of an ad-hoc 'uv run --with' invocation. I committed the exact 'same word, two types' error that AD-34 forbids, inside the enforcement code for that rule.
- (assumption) FALSE POSITIVE in R2-B: 'reviews/TRIAGE.md does not exist' - it exists; it was held aside in /tmp for the isolation protocol. An artifact of my own test setup, not a defect. Recorded because reviewer output must be verified, not accepted wholesale.
- (decision) CONVERGENCE ANALYSIS (both reviewers independently, therefore high confidence): target_ref granularity defeats AD-37's lock; Meeting minted twice from calendar vs watched folder (R2-B: privilege escalation); coverage-window clock mismatch + no 'unknown' commitment state; AD-36 attribution matching direction + missing default; event_log.md is per-scope so AD-31 disclosure records get git-committed to the EMPLOYER'S repo (the audit mechanism becomes the leak); 'reversible' is per-verb not per-provider; backup/restore vs storage tiers; AD-37's own Prevents text unclosed by its Rule. ~10 of ~20 pairs found by both.
- (change) pyproject.toml created. Both README-documented commands now work (previously both failed - no project definition, so every prior 'green' report was true only of an ad-hoc 'uv run --with' invocation). Runtime deps recorded under [project.optional-dependencies].runtime rather than [project.dependencies] so the architecture suite runs before the stack resolves. [tool.uv] python-preference='only-managed' encodes the F-11 prerequisite: sqlite-vec needs enable_load_extension, absent from python.org/system CPython.
- (decision) APPROACH CHANGE after run 2: stopped writing prose rules ABOUT types and wrote the types. pm_ai/domain/ is now real code - identity.py (DataScope vs SkillPermission, ONE SourceRef, TargetRef with pinned granularity, Actor + alias resolution), events.py (closed NormalizedEventType enum, Provenance with UNKNOWN, typed payload per event type enforced at construction), lifecycle.py (ProposalState/CommitmentState with an import-time disjointness assert, Verb registry keyed on (provider, verb) with reversible AND notifies, CoverageWindow, evaluate_commitment fail-closed). ~400 lines. Meaning is now held by the constructor, not by a reader's memory.
- (change) Convergent reviewer findings resolved IN CODE: 'scope' means 4 things -> DataScope vs SkillPermission, and a project named 'personal' no longer satisfies is_personal; two SourceRef grammars -> one type, bare URLs and ticket keys rejected; target_ref granularity -> sub-resource fragments rejected so AD-37's lock names one entity; closed type over open payload -> PAYLOAD_FOR registry enforced in __post_init__; authored_by two-valued and failing open -> Provenance.UNKNOWN, not admissible as evidence; 'reversible' per-verb -> per (provider, verb) plus a separate notifies flag, unregistered verbs fail closed; coverage gap -> CommitmentState.UNKNOWN never BROKEN.
- (change) Two defects the reviewers found in my own test suite, fixed: duplicate SourceRef types consolidated to pm_ai.domain.identity; two incompatible auth.classify() signatures unified. Tests now execute against real code rather than skipping - 21 passed / 41 skipped (was 12 passed / 44 skipped, and the passes were vacuous over empty __init__ files). Two tests failed on first real run and were genuine mismatches (exception name, unimplemented resolve_actor); both fixed. 12 contracts KEPT.
- (decision) AD-38 NEW - closes the disclosure leak both run-2 reviewers found independently. Root cause: two different record kinds conflated under one filename. Domain events (decisions, commitments, work-item activity) legitimately belong per scope and project scope is git-committed by design. Disclosure and cost records describe the ASSISTANT'S OWN behaviour, are never team-facing, and must be single and aggregatable - so they do not belong in event_log.md at all. They now live in ~/.pm-ai/disclosure.md, application scope, outside every repository. This simultaneously fixes the leak, makes AD-31's 'what left this machine' and AD-17's monthly total answerable from one file instead of N, and removes the routing ambiguity (an entry belongs to the scope owning its subject; an entry needing two scopes is two entries).
- (decision) AD-38 general invariant: no record written to a git-committed scope may reference personal-scope material - not by content, not by source_ref, not by scope name. Enforced in code via DataScope.is_git_committed + assert_writable(), not by prose. DisclosureRecord deliberately has NO scope field, so the routing decision that caused the leak cannot be made at all.
- (change) Spine updates for AD-38: AD-31 obligation 2 and AD-17 both redirected from event_log.md to the application ledger; AD-24 gains the per-scope routing rule; AD-1 class M row updated (and 'declared scopes' -> 'declared permissions', since SkillPermission is now a distinct type from DataScope); AD-3 Tier 1 lists disclosure.md. New pm_ai/domain/disclosure.py + 3 tests. 24 passed / 41 skipped, 12 contracts KEPT, spine lint 0.
- (question) PRD RECONCILIATION #7: PRD FR-27 states event_log.md stores 'all system decisions... and harvested telemetry events' and §2.1's tree shows event_log.md in both personal and project scopes with no disclosure ledger. The PRD does not yet reflect AD-38's split. Offer to update FR-27 + §2.1 when next editing the PRD.
- (decision) AD-5 REVISED (not a new AD - run 2's lesson was that my contradictions all came from treating batches as additions). Append-only now means NO FILE IS EVER EDITED IN PLACE, made compatible with FR-37 compaction via SEGMENTATION: each ledger is a directory of dated segments, exactly one open and appended to, earlier segments sealed and immutable. Compaction writes a NEW summary segment, records which sealed segments it supersedes, then may prune them - boundedness by replacing whole segments, never by rewriting lines. Readers fold across segments by (occurred_at, entry_id) per AD-35, so results don't depend on segment boundaries.
- (decision) AD-3 REVISED - tiers are now PHYSICALLY separated: Tier 1 markdown segments + disclosure.md + config; Tier 2 operational.db (SQLCipher); Tier 3 derived.db + vector_index/. 'reindex rebuilds Tier 3 only' becomes structurally true because Tier 2 is a different file. Backup set corrected from 'markdown only' to Tier 1 AND Tier 2 - the old rule would have lost the job queue, cursors, and executed-key ledger, state AD-3 itself says must survive and no rebuild can reconstruct.
- (decision) AD-3 - two boundaries on the zero-loss guarantee now STATED rather than implied: (1) NFR-11 scopes to Tier 3, and a rebuild after compaction reproduces the COMPACTED view, not pre-compaction detail - compaction is a deliberate recorded reduction; (2) restoring Tier 2 from backup opens a RE-EXECUTION WINDOW, because mutations after the backup point are absent from the restored executed-key ledger. Restore is a recovery event requiring an operator warning and external reconciliation; the idempotency key alone does not solve it. This closes reviewer finding D-14 honestly rather than pretending it is solved.
- (change) pm_ai/domain/storage_tiers.py: Tier enum with rebuildable/backed_up properties, ARTIFACT_TIER assigning every artifact exactly one tier, assert_reindex_safe() checking the artifact SET (so a reindex that grows a target cannot quietly acquire a Tier-2 file), plus an import-time assert that REBUILD_TARGETS and BACKUP_TARGETS are disjoint. 3 new tests. 27 passed / 41 skipped, 12 contracts KEPT, spine lint 0.
- (event) VERTICAL SLICE BUILT: GitLab harvest -> sanitize -> persist, plus a class-M mutation end to end. New modules: domain/harvest.py (opaque Cursor, HarvestResult carrying the coverage window in the RETURN TYPE so a connector cannot forget it), ports/ (ConnectorPort, StoragePort, SkillPort as protocols in domain types), core/sanitize.py + core/jobs.py, storage/service.py (single writer, markdown segments, Tier-2 separate), connectors/gitlab.py, skills/registry.py + skills/gitlab.py, app/wiring.py + app/pipelines.py. 43 passed / 37 skipped overall; 12 contracts KEPT; spine lint 0.
- (decision) THE ARCHITECTURE HELD. The composition root (AD-30) was necessary and sufficient - run_harvest touches connector, core, and storage, which no other layer may, and it wired cleanly. AD-9's one-method connector shape worked unchanged. Putting CoverageWindow in HarvestResult rather than leaving it to the scheduler means AD-35's fail-closed guard cannot be silently skipped - a shape improvement discovered by building, not by reviewing.
- (decision) AD-20 'idempotency key is HONOURED' now proven, not asserted: the registry checks the executed-key ledger BEFORE calling the skill and returns replayed=True with the prior external_id. Test asserts the underlying fake API received exactly ONE write across two invocations. This was the reviewer's highest-value missing check and was unwritable on paper.
- (decision) AD-36 closed loop exercised end to end: the executor's own comment is recorded in the executed-mutation ledger, and evaluate_commitment with PM_AI provenance yields BROKEN rather than FULFILLED. The worst finding of the review now has a failing-if-regressed test.
- (change) FINDING FROM BUILDING (not from any review): the connector called datetime.now() directly, making AD-35 coverage windows untestable deterministically - a fail-closed safety guard that cannot be tested cannot be trusted. Clock is now injected via the composition root; added a Clocks row to Consistency Conventions. This is the first defect found by writing code rather than by reviewing prose.
- (event) TRANSCRIPT SLICE BUILT. New: domain/meetings.py (Meeting as citation root + man_hour_cost for FR-03), domain/transcripts.py (TranscriptSource carrying the trust property as a TYPE, so authorization cannot be decided by inspecting a filename; Transcript.__post_init__ rejects an unbound meeting), domain/proposals.py (versioned CAS transitions), core/command_authorization.py, core/extraction.py, connectors/transcripts/{graph,manual}.py, storage proposal store (Tier 2), app pipeline run_transcript_ingestion. 66 passed / 31 skipped overall; 12 contracts KEPT; spine lint 0.
- (decision) PRIVILEGE-ESCALATION PATH VERIFIED CLOSED (reviewer R2-B D-6). Same PM, same reversible verb, same command text: GRAPH source -> executed=1 staged=0; MANUAL source -> executed=0 staged=1. The watched folder can ingest and stage but cannot confer execution authority. Checked directly, not only via the test suite.
- (decision) AD-32 auto-execution now proven across all eight cases incl. the one that matters most: gitlab:set_priority STAGES (reversible but notifies ~30 people, so one-tap undo is a lie) while jira:set_priority EXECUTES. Reversibility genuinely cannot be a property of the verb alone - the reviewer was right and the code now encodes it.
- (change) FINDING: the pre-existing AD-32 architecture tests called classify(source_authenticated, speaker_is_pm, verb) - no provider. That signature ENCODED THE BUG the reviewer found (verb-only reversibility). Writing the real implementation forced the correct signature and broke 6 tests, which is the enforcement layer working as intended: a test written against a guessed API can silently bless the wrong model until code exists.
- (decision) AD-33 verified end to end: every extraction from a transcript cites meeting:mtg_01HX, never the transcript. AD-29 verified through the full pipeline: hostile utterance retains its raw text while the model copy is cleansed. AD-13/AD-37 verified: implicit commitments always stage; concurrent approval raises VersionConflict; an expired proposal raises TerminalState.
- (event) RUN 4 RESUMED (finalize intent). Baseline re-verified before touching anything: 66 passed / 31 skipped, 12 import contracts KEPT, spine lint 0. Deliverables this run: spine only + refreshed SOLUTION-DESIGN.md.
- (change) BROWNFIELD RECONCILIATION - the spine had drifted from the code built in runs 3-4; 15 divergences found by reading pm_ai/ against the spine, not by review. THREE WERE SAFETY-CRITICAL, each a case where the spine still stated the bug the code had already fixed: (1) AD-31 obligation 2 still pointed the disclosure audit at event_log.md - the exact per-scope git-committed leak AD-38 was written to close; memlog entry 112 recorded this redirect but it landed only in AD-17, never in AD-31. (2) AD-32 still defined auto-execution by a 'reversible set' naming priority - the verb-only model the reviewer disproved and code replaced with (provider, verb) + notifies; gitlab:set_priority is the counterexample the spine still endorsed. (3) AD-36 still said authored_by is two-valued, the version that FAILS OPEN by defaulting unattributed events to external and counting them as fulfilment evidence.
- (change) Remaining 12 reconciliations, spine ratifying code rather than contradicting it: AD-9 harvest signature -> HarvestResult (events+cursor+coverage) with the reason the window rides in the return type; AD-14 gains CommitmentState.UNKNOWN + terminal-state rule; AD-13 gains EXECUTING (AD-37's CAS latch, previously referenced by AD-37 but absent from AD-13) and edited->superseded; AD-18 'scopes' -> SkillPermission, ending the word collision identity.py exists to prevent; AD-27 gains the closed-type-over-open-payload rule; AD-34 grammar now admits the scopeless global form (the rule as written rejected its own meeting: example) + TargetRef fragment rejection; AD-6 event_telemetry.db -> operational.db and states Tier-3 plaintext explicitly; event_log.md -> event_log/ segments normalized across AD-1/6/24/27/32/38, conventions and enforcement (AD-5 mandates segment directories while six other places still named a single file; AD-24's 'never rotated' now reconciled with FR-37 compaction); event envelope + entity-naming conventions rewritten to the real types incl. 'no bare Scope'; scopes diagram redrawn to the three physical tiers + disclosure.md; source tree gains connectors/transcripts/; stale 'not build-ready' risk replaced with the honest one - unexercised ADs are still prose.
- (assumption) VERIFIED r4 divergence claims myself rather than accepting them. CONFIRMED: (1) AD-36 IS DEFEATED IN CODE - the AD says attribution is established at both ends, but only end one exists. skills/registry.py:76 records every class-M mutation; NOTHING reads it back to mark harvested events as pm_ai. was_executed is consumed solely for AD-20 idempotency at registry.py:71. connectors/gitlab.py:51 hard-codes authored_by=Provenance.EXTERNAL unconditionally, so Provenance.UNKNOWN - the value AD-36 calls fail-closed - is unreachable on the only connector that exists. pm-ai posts a comment, harvests it, and counts it as evidence its own promise was kept. (2) assert_writable(stamped, scope=scope) at storage/service.py:71 is VACUOUS on NormalizedEvent: it reads getattr(record,'contributing_scopes',None) or (), an attribute NormalizedEvent does not have, so AD-38's git-committed-leak guard is a no-op on the only record type actually persisted. (3) StorageService._seen keys on natural_key=(system, source_ref) with NO scope component, so AD-38's mandated cross-scope split is silently swallowed as a duplicate. REFUTED: reviewer reported 65 passed/32 skipped; actual is 66/31, stable across three consecutive runs - my measurement, not theirs.
- (assumption) FURTHER DEFECT found while verifying, reported by no reviewer: StorageService._seen is an in-memory set on the instance, so AD-34's 'natural key makes re-harvest idempotent' holds only WITHIN a daemon lifetime. Across a restart the dedup set is empty and a replayed cursor window re-persists every event, doubling the metrics AD-34 exists to protect. The dedup ledger is Tier-2 state by AD-3's own definition (not derivable from Tier 1, must be backed up) and is currently not persisted at all.
- (version) VERIFIED INDEPENDENTLY (reviewer finding contradicted a cached source, so I checked live): the Cost-model caveat is FALSE. Anthropic made Sonnet 5's $2/$10 rate PERMANENT on 2026-08-10 and cancelled the 2026-09-01 increase to $3/$15. Every clause of the caveat - intro pricing, expiry date, standard pricing, 're-baseline in September' - is wrong, and it understates nothing. The claude-api skill's cached pricing table still carries the old framing; live sources win. Also: 'thinking on by default' and stop_reason='refusal' apply to Sonnet 5 too, not just Opus 5, so the spine exempts exactly the highest-volume briefing path from both hazards; uvicorn 0.52.4 shipped 2026-08-19.
- (event) REVIEWER GATE r4 COMPLETE: 5 parallel fresh-context lenses (rubric walker, tech-currency, adversarial divergence, spine-vs-code ratification, PRD reconciliation). Verdict: spine strong on structure, defeated on enforcement. CONVERGENCE: AD-36 found independently by THREE lenses and verified by me - highest confidence finding of the run. CROSS-LENS CONFLICT resolved by direct check: rubric walker confirmed the Sonnet 5 pricing caveat as correct while tech-currency called it false; I checked live vendor sources and tech-currency was right - the rubric walker had trusted the same stale cached table the spine did. Two lenses agreeing is not evidence when both read one source.
- (assumption) CRITICAL, REPRODUCED MYSELF rather than accepted: the enforcement layer does not enforce. I planted Path(p).open('w').write() in core/sanitize.py (an AD-5 single-writer violation) and subprocess.run(shell=True) in app/pipelines.py (an AD-1 execution-firewall violation), ran the suite, and got 66 passed / 12 contracts KEPT - fully green. Root causes: _write_mode reads the mode from args[1], the open(path,mode) form, so the idiomatic Path.open(mode) form is scored as a read; and pm_ai.app appears in neither the AST shell scan's layer list nor .importlinter's subprocess-confined contract, leaving the ONE layer permitted to import everything as the one layer unscanned for the firewall rule. Tree restored and verified clean; 66/31 and 12/12 confirmed after restore. The spine's claim that it is 'executable, not just readable' was false for its two most load-bearing rules.
- (change) SPINE FIXES APPLIED from the gate. Tightened: AD-36 now specifies the JOIN KEY (SourceRef.native_id == external_id within (system,scope)) and forbids a connector asserting 'external' at all - the missing mapping was structurally the same defect AD-34 exists to fix; AD-20 closes the crash window by requiring intent-before-call (in_flight -> executed) and provider-side tokens where offered, since a ledger appended after the call duplicates on the ORDINARY path not the rare one; AD-12 now enforces at the CONSUMER (ModelPort accepts only Sanitized for external text) because its Prevents names a consumer failure; AD-35 pins the coverage comparison to ingested_at with an explicit lag rule; AD-34 adds scope to the natural key and declares the dedup set Tier-2 (it was silently swallowing AD-38's mandated cross-scope split, and was in-memory so idempotency died at restart); AD-9 reconciled with the real two-member port (harvest + emits); AD-22 states that per-call budgets do NOT compose into the PRD's four end-to-end SLAs. NEW: AD-39 credential lifecycle - a wholly silent dimension where an expired token was indistinguishable from AD-35's coverage gap, so a dead connector read forever as patience; AD-40 push-occasion discipline - closes the PRD's own stated failure mode (the 'noisy notification relay') which no AD governed, and gives the SM-C1..C3 counter-metrics an enforceable home as a shared budget.
- (decision) AD-31 vs HR RESOLVED BY THE PM, and the resolution dissolved the conflict instead of trading it off. The two flows were never the same data: UJ-1 is PM<->pm-ai Socratic coaching (FR-12/14/15) where the PM is the SUBJECT - personal scope, never syncs to HR, AD-31.3 fully intact. UJ-4 is PM<->team-member 1:1 (FR-30/31) where a DIRECT REPORT is the subject - that material is meant to sync to HR, and the PRD already says so ('upon his explicit approval, pm-ai syncs the agreed goals to the HR platform via MCP'). No AD needed weakening.
- (decision) GAP EXPOSED BY THAT CLARIFICATION, missed by all four review lenses because they checked the spine against itself: the three-scope model has NO valid home for a direct report's career data. Not ~/.manager-ai/ (sovereign, must not travel to the next employer, and its charter is that it never reaches HR); not <repo>/.project-ai/ (git-committed - the report's PEERS could read their performance objectives); not ~/.pm-ai/ as defined ('no personal records'). The tell was already in the PRD: team_member_career_mcp.py, the HR sync connector, is filed under ~/.manager-ai/skills/ - inside the one scope whose entire charter forbids HR sync. RESOLUTION (PM chose sub-scope over a fourth top-level scope): people data lives at ~/.pm-ai/private/people/, encrypted and gitignored. I added the refinement that it is a distinct SCOPE KIND in the type system regardless of physical location - because DataScope(APPLICATION) would otherwise cover both disclosure.md and a report's dossier, leaving AD-31's destination rule unable to permit people->HR while forbidding personal->HR. Same reasoning that split DataScope from SkillPermission in run 3. is_personal=false, is_git_committed=false. Accepted tradeoff, stated in AD-4: the application scope no longer holds 'no personal records'; compensating obligation is that people/ is a single deletable directory so leaving a role is one removal.
- (change) Spine updated for the people scope: AD-4 revised with the personal-vs-people table (whose data, source flow, HR egress, survives-company-change) and the deletable-directory obligation; AD-31 gains the two-1:1s table and states personal material may never be a source for a people->HR sync, not even via a model that read both; AD-28 becomes three entities - Commitment (project) / CoachingCommitment (personal) / CareerGoal (people), with CareerGoal explicitly NOT a Commitment since it is not verified against commit telemetry; AD-25 separates FR-30 per-employee metrics from the personal analytics DB in both directions; AD-38's cross-scope invariant extends to people (a report's objective in a committed repo is readable by their peers); scopes diagram gains private/people/ plus the two HR edges - one permitted and PM-approved, one prohibited; conventions gain a Scope-kinds row; capability map FR-30/31 row rebound; the AD-31/HR Open Risk is removed as resolved.
- (change) ALL FIVE GATE DEFECTS FIXED IN CODE (PM authorized extending scope past the spine). Each was demonstrated RED against its own pre-fix code before the fix was accepted - the discipline I had just written into the spine as 'a check that cannot be shown to fail on a planted violation is not a check'. (1) AD-5 bypass: _write_mode read the mode from args[1], the builtin open(path,mode) position, so the idiomatic Path.open('w') scored as a READ; now handles both call shapes. (2) AD-1 bypass: pm_ai.app was in neither the AST shell scan nor .importlinter's subprocess-confined contract - the composition root, the one layer permitted to import everything, was the one layer unscanned; now in both. (3) BONUS HOLE found by the plant and by NO reviewer: call names were never resolved through imports, so 'import subprocess as _sp; _sp.run(shell=True)' matched no forbidden-call entry; conftest now resolves aliases to their origin. (4) AD-36 wired: new pm_ai/core/normalize.py does the ledger match with the join key AD-36 now states (SourceRef.native_id == external_id within (system,scope)), connector emits Provenance.UNKNOWN and may never assert external, pipeline calls attribute_all before persist. (5) Tier 2 durable: operational.db (SQLite, WAL) replaces four in-memory dicts, carrying cursors, coverage, executed ledger, seen-set and proposals. (6) dedup key gains scope and is persisted. (7) assert_writable generalized via referenced_scopes so it actually guards NormalizedEvent. (8) AD-20 ordering: begin_execution claims the key BEFORE the provider call, settle after; a key found in_flight raises ReconciliationRequired.
- (decision) TEST-QUALITY LESSON, recorded because it recurred: my first AD-20 crash-window test PASSED against the reverted record-after-call code. It exercised the storage primitive rather than the registry's ORDERING, so it could not see the defect it was written for - the same shape as the pre-existing AD-36 test that passed while AD-36 was defeated in code. Replaced with a test that makes the provider accept the write and then raises, asserting the retry does NOT reach the provider a second time; that one fails on the old ordering. Also added tests/architecture/test_enforcement_meta.py - the AST helpers had NO tests, which is precisely how two load-bearing checks became bypassable without anything turning red. A check nobody checks is a comment. Also repointed two checks that imported modules the code never created (pm_ai.core.taxonomy, pm_ai.core.commitments.CommitmentStatus): they had been skipping forever while reading as covered, and AD-14's now runs. Final: 92 passed / 30 skipped, 12 contracts KEPT, spine lint 0.
- (change) SOLUTION-DESIGN.md rewritten. It had drifted harder than the spine and three of its claims were the PRE-revision architecture, still readable as current: (1) 'Delete the database and the vector store, run pm-ai reindex, and you lose nothing' - true only before Tier 2 existed; following it today destroys the job queue and every cursor; (2) MCP skills as 'the single egress point, nothing else may mutate' - the overclaim AD-1 deliberately retired for five classified classes; (3) 'addressing the assistant by name IS the authorization' - the pre-AD-32 model, and the most dangerous sentence in the document, since anyone in the meeting can say the name and anyone who can write to the watched folder can put words in anyone's mouth. Also fixed: section 8 repeated the disproved SQLCipher rationale that section 2 of the same document corrects; the cost table carried the retracted pricing. New: the two-1:1s table and the people scope, the reviewer-gate lessons section, and a 13-row divergence table (was 6) all now reconciled in PRD v0.11.0.
- (event) spine finalized - status: final, 40 ADs, sources bumped to prd v0.11.0, companions now names SOLUTION-DESIGN.md (it was [] while the companion sat beside it contradicting AD-3 verbatim). Final state: spine lint 0 findings, 92 passed / 30 skipped, 12 import contracts KEPT, 4 valid mermaid diagrams. Run 4 closed all 12 triage clusters from run 1, the run-2 seam contradictions, and the r4 gate's five verified code defects.
- (event) doc_standards polish applied to SOLUTION-DESIGN.md only (never the spine): lessons section moved out from between Risks and Roadmap - it broke the how-it-works -> risks -> roadmap arc - and retitled from a self-justifying header to what it contains; 9 prose cuts. Diagrams and all 41 table rows intact. Note: the first polish subagent died on a session limit before editing, so this pass was done inline; the file was verified unmodified first.
- (decision) PM DIRECTIVE: remove ~/.manager-ai-private/, a fifth top-level location holding exactly one file, and fold personal_analytics.db into ~/.manager-ai/private/ keeping every original constraint (encrypted, gitignored, away from project data). Done - and the move STRENGTHENS the boundary rather than just relocating it. The old directory belonged to no scope kind, so it sat outside AD-4's ownership rules and AD-38's cross-scope invariant, which are the two mechanisms that actually stop personal material reaching a committed artifact; it was governed by neither and protected only by being somewhere else. Inside the personal scope it is covered by both, and the separation that carries AD-25 - a distinct database project rendering has no code path to open - is unchanged.
- (assumption) TWO LATENT DEFECTS surfaced while making that move, neither of them part of the request. (1) personal_analytics.db had NO STORAGE TIER AT ALL - absent from ARTIFACT_TIER, so it was in neither BACKUP_TARGETS nor REBUILD_TARGETS. The single store holding months of personal burnout and workload history was the one artifact no backup covered, and AD-3's 'every artifact falls in exactly one tier' was false. (2) AD-6's encrypted set OMITTED it while AD-25 described it as an encrypted store - two ADs disagreeing about the same file. Both fixed.
- (decision) TIER ASSIGNMENT (my judgment call, flagged to the PM): personal_analytics.db is TIER 2, not Tier 3, despite AD-25 describing it as 'derived telemetry'. That word means CALCULATED; Tier 3's test is narrower - rebuildable from Tier 1 WITH ZERO LOSS. Burnout and workload trends are longitudinal and outlive their inputs, because FR-37 compaction prunes the telemetry they were computed from, so a rebuild after compaction returns a shorter history rather than the same one. It fails the Tier-3 test, so it is durable, backed up, and never a rebuild target. This is the same word-collision family as scope-meaning-four-things and derived-meaning-two: 'derived' as a calculation vs 'Derived' as a durability class. Encryption and tier are now stated as INDEPENDENT axes - confidentiality vs durability - with this file as the proof case, since AD-6's Tier-3-is-plaintext rationale ('embeddings, not recoverable text') never covered recoverable personal facts.
- (change) Applied across all artifacts: spine AD-25 rewritten (new path + the wall-is-the-scope-boundary rationale), AD-6 encrypted set now names the file and states the encryption/tier orthogonality, AD-3 gains the Tier-2 row plus the reasoning and the had-no-tier admission, scopes diagram drops the PERSPRIV subgraph into the personal scope with a PROJ-cannot-open edge, backup line adds the file and the gitignore-private/ caveat for a personal-scope repo. Code: storage_tiers.py gains the entry. Tests: test_ad25 repointed - it grepped for 'manager-ai-private', a string that after this move can never appear, so it would have passed VACUOUSLY the day it stopped skipping; now asserts against the personal scope. Two new running tests cover the tier, backup membership, and that reindex cannot reach it. 94 passed / 30 skipped, 12 contracts KEPT, spine lint 0.
- (decision) PM DIRECTIVE: chat_history/ and telegram_cache/ sit in ~/.pm-ai/, a scope documented as holding NO personal records, and both are personal - move them to .manager-ai. I PUSHED BACK ON HALF of this and the PM took the alternative. telegram_cache is the PM's own voice notes and dialogue state -> personal scope, agreed. But NFR-09 defines chat_history as RAW MEETING TRANSCRIPTS: employer material, not personal. Moving it to the sovereign scope would have (a) put project-specific content in the one scope AD-4 defines as carrying none, (b) carried a former employer's meeting recordings to the PM's next job, since that scope is built to survive company transitions, and (c) forced project-scope transcript ingestion to read the personal scope on every meeting, which is exactly what AD-25 forbids.
- (assumption) LIVE AD-38 VIOLATION found by pulling that thread, and it was in the FINALIZED spine. meetings/ was filed in ~/.manager-ai/ (personal) while AD-33 makes Meeting the citation root and commitments live in <repo>/.project-ai/memory/commitments_log.md, which is git-committed. So EVERY commitment extracted from a meeting referenced personal-scope material by source_ref - the exact phrasing AD-38 prohibits ('not by content, not by source_ref, not by scope name'). It survived four r4 review lenses and my own reconciliation because assert_writable checks the scope a record BELONGS TO and never the scope it POINTS AT. Also found: the PRD's .gitignore reserved .project-ai-private/ and nothing had ever lived in it - a dangling reservation that was the correct home for raw captures all along.
- (decision) RESOLUTION (PM chose per-scope over the simpler move): an artifact belongs to the scope that owns its SUBJECT - the rule AD-38 already stated for log entries, now general. A Meeting carries a required scope field; a transcript is stored in its meeting's scope and never one of its own, because the capture cannot be more or less shareable than the event it records. Team meeting -> project (record committed, raw capture in <repo>/.project-ai-private/); 1:1 with a direct report -> people; personal session -> personal. Meeting.scope is required rather than defaulted because it decides two things no caller may guess: where the transcript is written, and whether a git-committed record may cite that meeting at all. New assert_citation_legal() checks the citation DIRECTION, complementing assert_writable's belongs-to check; wired into run_transcript_ingestion before extraction. Both new tests fail with the guard removed.
- (decision) RAW CAPTURES ARE OUTSIDE THE TIER MODEL, deliberately and assertedly. chat_history/ and telegram_cache/ are transient input the pipeline consumes and NFR-09 purges at 30 days. They are NOT Tier 3: Tier 3 promises rebuildable-from-Tier-1-with-zero-loss and no rebuild reconstructs a recording. New RETENTION_MANAGED frozenset with an import-time assert that it is disjoint from ARTIFACT_TIER. The point is the difference between an artifact ABSENT from every set (an oversight - that is how personal_analytics.db came to be backed up by nothing, found yesterday) and an artifact NAMED as excluded (a decision a check keeps honest). Never a backup target in any scope; AD-33 already forbids anything depending on them.
- (change) Applied: spine AD-33 (Meeting gains required scope + the AD-38 violation admission), AD-23 (transcript stored in its meeting's scope; project captures in .project-ai-private/), AD-4 (project scope gains its uncommitted sibling, previously reserved and never used), AD-6 (encrypted set per scope), AD-3 (raw captures excluded from the tier model on purpose), scopes diagram redrawn with PROJPRIV and the citation edge, backup line excludes raw captures. Code: Meeting.scope + transcript_home, assert_citation_legal, RETENTION_MANAGED, pipeline guard. Tests: 4 new (2 citation-direction, 1 transcript-home, 1 retention-managed), both citation tests proven red with the guard removed. 98 passed / 30 skipped, 12 contracts KEPT, spine lint 0.
- (decision) PM STRUCTURAL FEEDBACK, both points accepted. (1) chat_history/ renamed transcripts/ - it was neither a chat nor a history, and a name describing nothing in the system costs every reader a decode. (2) .project-ai-private/ retired after less than a day: captures now live at <repo>/.project-ai/transcripts/, one directory per scope rather than a second top-level directory per project. The PM's reasoning was the same instinct that retired ~/.manager-ai-private/ yesterday - a whole directory tree existing to hold one thing - and it applies identically here. Every scope now keeps its captures at the same relative path, the way each keeps its own event_log/.
- (decision) COST OF THAT FOLD, handled rather than left implicit: transcripts/ now sits INSIDE a committed scope, so its exclusion from git rests on a .gitignore rule instead of a directory boundary - and a rule can go missing in a way a boundary cannot. Failure mode is publishing verbatim meeting minutes to the employer's repository, the same class of leak as AD-38 but arriving by omission rather than by routing. Added GITIGNORE_REQUIRED + assert_capture_dir_ignored(): the daemon verifies the rule before writing a capture and REFUSES if absent. Fails closed on the right side - losing a transcript is recoverable since AD-33 forbids anything depending on one, while committing a transcript is not.
- (change) Applied: spine AD-23, AD-4, AD-6, AD-3, scopes diagram (PROJPRIV folded into PROJ as R4), backup line. Code: RETENTION_MANAGED renamed, GITIGNORE_REQUIRED + assert_capture_dir_ignored + UnprotectedCaptureDir. PRD: tree, 2.1 narrative, 6 topology, NFR-08, NFR-09, and the v0.12.0 Decisions Log entry AMENDED rather than superseded - it was written earlier the same day and described a .project-ai-private/ layout that never shipped, so leaving it would have documented a destination that never existed. 99 passed / 30 skipped, 12 contracts KEPT, spine lint 0.
- (assumption) PM FOUND A LOST PILLAR, and was right about the loss but not the location: FR-11 (Micro-Decision Daily Alignment Engine) is INTACT in the PRD - it was the ARCHITECTURE that dropped it. In the spine the alignment engine survived only as a directory name (core/alignment) and as three characters inside an FR-range in the capability map ('Coaching, briefings, alignment, anti-burnout (FR-09..FR-17)') governed by AD-15/17/25/28, none of which concerns goal alignment. No AD covered the goal horizons, strategic_goals.md as source of truth, or the [Strategic Alignment] tag. This is the exact failure the r4 PRD-reconciliation lens flagged generically as H2-6 (product principles the AD structure drops) - an FR-RANGE IN A TABLE READS AS COVERAGE WHILE FIXING NOTHING. Continuous alignment of daily micro-decisions across goal horizons is what the product IS, and it had no invariant.
- (decision) AMBIGUITY FOUND WHILE WRITING AD-41, present in the PRD from the start and load-bearing: <Tier> named TWO ORTHOGONAL AXES. FR-11 said short/medium/long, 2.1 said Project/Team/Personal ('3-Tier Goals'), UJ-9 said Strategic/Tactical/Operational. These are 'what a goal is about' vs 'when it lands' - a project goal may be long-horizon and a personal goal short-horizon. Two builders would resolve [Strategic Alignment: <Tier>] differently and BOTH be compliant with the requirement as written. Resolved as two separate closed enumerations in domain: GoalDomain (project|team|personal) IS the tag value, matching 2.1's own '3-Tier' wording and the PM's framing; GoalHorizon (short|medium|long) is what UJ-9's planning breakdown groups by. Flagged to the PM as my judgment call - it is reversible by swapping which enum feeds the tag.
- (decision) AD-41 core rules beyond the taxonomy: (1) NO RECOMMENDATION IS SURFACED AS ALIGNED WITHOUT A RESOLVABLE goal:<id> - a task the engine cannot align renders explicitly UNALIGNED, never untagged, because aligned-by-omission produces a briefing that reads as strategic while its tags mean nothing, and alignment is the claim the product is sold on. (2) strategic_goals.md is hand-editable Tier-1 markdown, so a citation to a deleted goal must surface as unresolved rather than silently degrade - AD-34's actor rule applied to goals, for the same reason: a reference that quietly becomes nothing corrupts every metric built on it. (3) the Rationale Snippet is generated prose and never substitutes for the citation - AD-33's logic one level up. (4) strategic_goals.md is personal scope, so the tag belongs on personal-scope surfaces; project-scope alignment would need project-scope goals, which do not exist - DEFERRED rather than silently decided, since a project artifact citing a personal goal is the AD-38 violation that moved meetings.
- (change) AD-41 added (41 ADs). New pm_ai/domain/goals.py: GoalDomain + GoalHorizon closed enums, Goal with stable goal_ id and source_ref, Recommendation with a REQUIRED aligned_to so 'forgot to align' cannot look like 'could not', resolve() raising UnresolvedGoal, alignment_tag() emitting UNALIGNED rather than nothing. 6 new tests. WRITING THE TESTS CAUGHT A REAL GAP: goal:goal_01 did not parse, because AD-34's scopeless global set was closed and contained only 'meeting' - the closed-enumeration discipline working exactly as designed, catching an invented reference form at the boundary. Added 'goal' to _SCOPELESS and to AD-34's text. Capability map now breaks FR-11 out of the range that hid it; Identifiers convention gains goal_; Deferred gains project-scope goals. 105 passed / 30 skipped, 12 contracts KEPT, spine lint 0.
- (decision) PM ADDITION: alignment must DRIVE RANK, not merely label. An aligned task is promoted because a citation to a goal is evidence the work advances a stated growth path; unaligned tasks may pull strategically in a direction nobody chose, though they are still important. Extended AD-41 in place (rules 6 and 7) rather than minting AD-42 - run 2's recorded lesson was that my contradictions all came from treating batches as additions, and ranking-by-alignment is the same subject as alignment. One ordering rule across FR-09, FR-13 and FR-32; a surface inventing its own is a defect.
- (decision) TWO GUARDS I ADDED AND FLAGGED AS INTERPRETATION. (1) Alignment LIFTS BUT DOES NOT OVERRIDE urgency. The PM said aligned tasks are 'promoted to the top'; taken literally as a sole primary key, an unaligned production incident would sort below a long-term refactor. A ranking that buries genuine urgency behind strategy is one the PM stops trusting after the first outage, and the PM's own words said unaligned tasks 'are also important'. So: urgency leads, alignment breaks the tie. Reversible if pure alignment-first is wanted. (2) UNALIGNED WORK IS NEVER HIDDEN - it ranks lower but stays individually visible and is surfaced AS A SET, because a week of unaligned work is exactly what FR-24's drift audit exists to catch and burying it destroys the only evidence of drift. This is the sharper half of the PM's note: unaligned is a signal, not noise.
- (decision) ANTI-GAMING COROLLARY, unprompted but necessary: making alignment raise rank creates an incentive to tag everything aligned, which would empty the tag of information - the classic optimize-the-metric failure the SM-C counter-metrics exist to name. AD-41.7 therefore states that nothing may mark a task aligned in order to promote it; rank follows a RESOLVABLE goal:<id> citation (rule 2), never a model judgement that something feels strategic. The citation hardening written an hour earlier is what makes the ranking rule safe to add at all.
- (change) goals.py gains rank_key(), order() and unaligned(). order() is built on stable sorted() so items equal on both keys keep the caller's order - no hidden third criterion. urgency is supplied by the caller rather than defined here, since urgency is not this module's to own. 5 new tests incl. the one that matters most: an unaligned prod incident outranks an aligned refactor. 110 passed / 30 skipped, 12 contracts KEPT, spine lint 0.
